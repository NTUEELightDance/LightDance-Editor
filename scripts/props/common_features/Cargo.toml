[package]
name = "common_features"
version = "0.1.0"
edition = "2021"

[lib]
name = "common_features"
path = "src/common_features.rs"

[dependencies]
# Uncomment when needed for JSON serialization:
# serde = { version = "1.0", features = ["derive"] }
# serde_json = "1.0"
