[package]
name = "file-server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8.1"
dotenv = "0.15.0"
tokio = { version = "1.32.0", features = ["rt-multi-thread"] }
tower-http = { version = "0.5.2", features = ["fs"]}
