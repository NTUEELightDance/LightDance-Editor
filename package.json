{
  "name": "2021-lightdance-editor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:editor": "yarn --cwd ./editor",
    "install:editor-server": "yarn --cwd ./editor-server",
    "install:controller-server": "yarn --cwd ./controller-server",
    "install:file-server": "yarn --cwd ./file-server",
    "install:all": "(yarn install:editor); (yarn install:editor-server); (yarn install:controller-server); (yarn install:file-server)",
    "dev:file-server": "cd ./file-server && yarn start",
    "dev:editor": "cd ./editor && yarn start",
    "dev:editor-server": "cd ./editor-server && yarn start",
    "dev:controller-server": "cd ./controller-server && yarn start"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
